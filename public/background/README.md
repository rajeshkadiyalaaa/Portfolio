# Section background art

Ink-wash corner PNGs for About, Background, and Contact sections.

- **Canonical location:** `public/background/` only (legacy `public/about/*.png` duplicates were removed).
- Edit files here, then bump `STATIC_ASSET_VERSION` in `src/lib/staticAssets.ts`.
- These assets are **not** generated by `npm run optimize:assets`.
